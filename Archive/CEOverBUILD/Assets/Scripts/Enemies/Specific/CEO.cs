using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CEO : Enemy {

    private void Start()
    {
        GameObject.FindGameObjectWithTag("GameManager").GetComponent<GameManager>().enemyList.Add(gameObject);
    }

    // Update is called once per frame
    void Update () {
		

























































































































































































































































        if(Input.GetKey(KeyCode.M) && Input.GetKey(KeyCode.A) && Input.GetKey(KeyCode.T))
        {
            Instantiate(thing, transform.position + transform.forward * 1.5f + Vector3.up * 3 + transform.right * -1 * 6.5f,Quaternion.identity).transform.GetChild(0).GetComponent<TextMesh>().text = "Matt is cool, and if you're seeing this, you're probably cool too";
            Debug.Log(":]");
        }
	}

    public GameObject thing;
}
